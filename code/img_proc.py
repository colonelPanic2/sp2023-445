
class image_proc:
    def __init__(self,region_array=[0,0,0,0,0,0]):
        # This array will keep track of where the ball is located in the 6 regions
        # Of the camera view. The ball can be in multiple regions at once.
        self.region_array=region_array
        # Initialize other necessary 
    def check_ball_inview(some_variables=None):
        if some_variables is not None:
            return 'ball in camera view'
        return 'ball NOT in camera view'

    def check_ball_position():
        return
